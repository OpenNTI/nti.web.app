const Ext = require('@nti/extjs');

require('../tiles/Note');


module.exports = exports = Ext.define('NextThought.app.stream.components.listTiles.Note', {
	extend: 'NextThought.app.stream.components.tiles.Note'
});
